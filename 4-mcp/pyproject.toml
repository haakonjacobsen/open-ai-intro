[project]
name = "mcp-workshop"
version = "0.1.0"
description = "MCP Server Workshop"
requires-python = ">=3.10"
dependencies = [
    "mcp>=1.0.0",
    "pydantic-ai>=0.0.14",
    "python-dotenv>=1.0.0",
]
